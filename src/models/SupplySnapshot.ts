export interface SupplySnapshot {
  player: string;
  cardSet: string;
  year: number;
  product: string;
  parallel: string;
  activeSupply: number;
  supplyTrend2W: number;
  supplyTrend4W: number;
  supplyTrend3M: number;
  timestamp: string;
}
