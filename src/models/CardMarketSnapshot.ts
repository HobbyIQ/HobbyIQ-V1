export interface CardMarketSnapshot {
  player: string;
  cardSet: string;
  year: number;
  product: string;
  parallel: string;
  grade: string;
  isAuto: boolean;
  estimatedValue: number;
  compCount: number;
  timestamp: string;
}
