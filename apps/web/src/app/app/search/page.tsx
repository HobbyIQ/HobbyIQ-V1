import { ComingSoon } from "@/components/ComingSoon";

export default function SearchPage() {
  return (
    <ComingSoon
      title="Search"
      body="Look up any card by name, cert number, or player. Empirical FMV across the full grade ladder."
    />
  );
}
