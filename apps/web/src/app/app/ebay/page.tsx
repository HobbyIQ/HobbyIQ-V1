import { ComingSoon } from "@/components/ComingSoon";

export default function EbayPage() {
  return (
    <ComingSoon
      title="eBay integration"
      body="Connect your eBay account for one-click listing drafts from any holding, automatic sales sync back into portfolio + comp pool, and shipping-policy management. Connect flow + listing UI landing in Sprint 3."
    />
  );
}
