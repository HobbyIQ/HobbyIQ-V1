import { ComingSoon } from "@/components/ComingSoon";

export default function ErpPage() {
  return (
    <ComingSoon
      title="Pro Seller ERP"
      body="Bulk inventory workflows: CSV/Excel import, batch edit, batch price refresh, tax-ready reports (Schedule D), sale reconciliation. Built for volume sellers. Landing in Sprint 4."
    />
  );
}
