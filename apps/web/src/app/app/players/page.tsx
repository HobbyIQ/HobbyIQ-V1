import { ComingSoon } from "@/components/ComingSoon";

export default function PlayersPage() {
  return (
    <ComingSoon
      title="Players"
      body="Player-first view: top movers, season stats, matched-cohort momentum, price history per player. Deep-links from search results in the next sprint."
    />
  );
}
