import { ComingSoon } from "@/components/ComingSoon";

export default function AlertsPage() {
  return (
    <ComingSoon
      title="Alerts"
      body="Price alerts, grade-worthy alerts, sub-raw prospect alerts, watchlist-trigger alerts. Set thresholds, get notified in-app or via push. Backend routes exist — UI in the next sprint."
    />
  );
}
