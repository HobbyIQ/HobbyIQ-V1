name: Checklist Acquisition

# CF-CHECKLIST-ACQUISITION (Drew, 2026-08-16: "lets get every catalog we need
# and ingest the FULL checklist for all of those and that way we can run it
# daily to stay on top of it" / "let's build the daily job at 5 am 9 am 1 pm
# and 5 pm").
#
# Two steps, every run:
#   1. checklist-gap-report  — rank every product our SALES reference by how
#                              many sales are stranded with no CHECKLIST behind
#                              them.
#   2. acquire-missing-checklists — fetch and ingest the top of that ranking.
#
# WHY NOT DRAIN THE SEED QUEUE. That queue is fed by match FAILURES, and on
# 2026-08-16 only 22% of 478 diagnosed failures were genuinely missing a
# checklist. Its top entry asked for 2025 Topps against 2,944,147
# checklist-backed rows we already owned. A work list built from failures
# inherits the failures and spends publisher goodwill on things we have.
#
# The gap report asks the question directly instead, which is how Donruss Optic
# surfaced: 207,638 comps against 1,468 checklist rows — the single largest
# coverage gap in the pool, and invisible to the queue.
#
# SELF-LIMITING. The ranking is recomputed every run, so a product drops off
# the list once it is covered. The job cannot loop on the same target, and it
# does nothing at all when there is nothing missing.
#
# BOUNDED PER RUN. max=12 products. Each is a real page fetch from an external
# publisher plus a full ingest; a manual sweep of 48 seeds produced 11
# transient ECONNABORTED aborts, and pulling harder raises the failure rate
# rather than the throughput. Four runs a day at 12 is ~48 products/day, which
# clears a backlog of this size in weeks while staying a polite neighbour.

on:
  schedule:
    # 5am / 9am / 1pm / 5pm ET. GitHub cron is UTC and has no DST, so these are
    # pinned to EDT (UTC-4) and land an hour later in winter — the same
    # convention the other scheduled jobs in this repo use.
    - cron: '0 9,13,17,21 * * *'
  workflow_dispatch:
    inputs:
      max_products:
        description: 'Products to acquire this run'
        required: false
        default: '12'
      min_comps:
        description: 'Ignore products with fewer stranded sales than this'
        required: false
        default: '800'
      apply:
        description: 'Write to the catalog (false = report only)'
        required: false
        default: 'true'
      # DIRECT-URL LANE (CF-HM-DIRECT-URL, 2026-08-30). The ranked lane finds a
      # release by asking hmSlugFor to match a gap onto the --list index, and
      # that index does not hold every page: 2026-bowman-baseball-checklist is
      # absent from it, and 'bowman'+2026+baseball matches two OTHER slugs, so
      # the ambiguity refusal fires before the missing slug is even noticed. A
      # release we can name by URL should not be unreachable because an index
      # is thin. Given release_url, this lane fetches that exact page and
      # ingests it, bypassing hmSlugFor entirely.
      #
      # The cron lanes never set these, so they are untouched.
      release_url:
        description: 'DIRECT LANE: hobbymonitor release URL (skips the gap ranking entirely)'
        required: false
        default: ''
      set_key:
        description: 'DIRECT LANE: catalog setKey, e.g. bowman (must be the key the catalog ALREADY uses)'
        required: false
        default: ''
      set_name:
        description: 'DIRECT LANE: display name, e.g. "2026 Bowman"'
        required: false
        default: ''
      year:
        description: 'DIRECT LANE: product year, e.g. 2026'
        required: false
        default: ''
      sport:
        description: 'DIRECT LANE: sport, e.g. baseball'
        required: false
        default: 'baseball'

concurrency:
  group: checklist-acquisition
  cancel-in-progress: false

jobs:
  acquire:
    name: Acquire missing checklists
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install backend deps
        working-directory: backend
        run: npm ci

      # CF-DRAIN-NEEDS-DIST (2026-08-16). The seed-queue job shipped without
      # this and went green while ingesting NOTHING — ingest-scraped-checklist
      # require()s compiled modules out of dist/, so every child died on a
      # missing module and the parent still exited 0. Never omit the build.
      - name: Build backend
        working-directory: backend
        run: npm run build

      - name: Login to Azure
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_1259D9D213E34AE29B2DD54B3E755C4D }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_5AB8B3DC612348FD9D37DBE28FF40802 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_A45CF8552AE94AF68A046FB846E21206 }}

      - name: Rank what is missing
        if: ${{ !inputs.release_url }}
        working-directory: backend
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'
        run: |
          COSMOS_CONNECTION_STRING=$(az webapp config appsettings list \
            --name HobbyIQ3 --resource-group rg-hobbyiq-dev \
            --query "[?name=='COSMOS_CONNECTION_STRING'].value" -o tsv | tr -d '\r\n')
          if [ -z "$COSMOS_CONNECTION_STRING" ]; then echo "COSMOS_CONNECTION_STRING empty"; exit 1; fi
          COSMOS_CONNECTION_STRING="$COSMOS_CONNECTION_STRING" \
          COSMOS_DATABASE=hobbyiq \
          node scripts/checklist-gap-report.cjs \
            --min-comps="${{ inputs.min_comps || '800' }}" \
            --top=60 \
            --json=/tmp/gaps.json

      - name: Fetch and ingest
        if: ${{ !inputs.release_url }}
        working-directory: backend
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'
        run: |
          COSMOS_CONNECTION_STRING=$(az webapp config appsettings list \
            --name HobbyIQ3 --resource-group rg-hobbyiq-dev \
            --query "[?name=='COSMOS_CONNECTION_STRING'].value" -o tsv | tr -d '\r\n')
          APPLY_FLAG=""
          if [ "${{ inputs.apply || 'true' }}" = "true" ]; then APPLY_FLAG="--apply"; fi
          COSMOS_CONNECTION_STRING="$COSMOS_CONNECTION_STRING" \
          COSMOS_DATABASE=hobbyiq \
          node scripts/acquire-missing-checklists.cjs $APPLY_FLAG \
            --max="${{ inputs.max_products || '12' }}" --gaps=/tmp/gaps.json

      # ── DIRECT-URL LANE ────────────────────────────────────────────────
      # Runs ONLY when release_url is given, and then it is the whole job: the
      # ranked steps above are skipped. Refusal gates come before any fetch
      # (#1565) — an ingest under the wrong setKey mints a second, parallel
      # product beside the real one, which is worse than the gap it closes.
      - name: Acquire one release by URL
        if: ${{ inputs.release_url }}
        working-directory: backend
        env:
          NODE_OPTIONS: '--max-old-space-size=4096'
          RELEASE_URL: ${{ inputs.release_url }}
          SET_KEY: ${{ inputs.set_key }}
          SET_NAME: ${{ inputs.set_name }}
          YEAR: ${{ inputs.year }}
          SPORT: ${{ inputs.sport }}
          APPLY_INPUT: ${{ inputs.apply }}
        run: |
          set -euo pipefail

          # ---- refusal gates, before any network call or require ----
          case "$RELEASE_URL" in
            https://www.hobbymonitor.com/release/*) ;;
            *) echo "REFUSE: release_url must be a https://www.hobbymonitor.com/release/... URL (got: $RELEASE_URL)"; exit 1 ;;
          esac
          if [ -z "${SET_KEY:-}" ]; then echo "REFUSE: set_key is required — without it the manifest cannot name the product"; exit 1; fi
          if [ -z "${YEAR:-}" ]; then echo "REFUSE: year is required"; exit 1; fi
          case "$YEAR" in ''|*[!0-9]*) echo "REFUSE: year must be numeric (got: $YEAR)"; exit 1 ;; esac
          if [ -z "${SPORT:-}" ]; then echo "REFUSE: sport is required"; exit 1; fi
          # A prettified setKey mints a parallel product. Refuse anything that
          # is not already a lowercase slug.
          case "$SET_KEY" in
            *[!a-z0-9-]*) echo "REFUSE: set_key must be a lowercase slug like 'bowman' (got: $SET_KEY)"; exit 1 ;;
          esac
          SET_NAME_EFF="${SET_NAME:-$YEAR $SET_KEY}"

          echo "== DIRECT LANE =="
          echo "  url=$RELEASE_URL"
          echo "  year=$YEAR setKey=$SET_KEY setName=$SET_NAME_EFF sport=$SPORT"
          echo "  apply=${APPLY_INPUT:-true}"

          OUT="data/checklists/scraped/acquired/${YEAR}-${SET_KEY}-${SPORT}.csv"

          node scripts/fetchHobbyMonitorChecklist.cjs \
            --url "$RELEASE_URL" \
            --out "$OUT" \
            --year "$YEAR" \
            --set-key "$SET_KEY" \
            --set-name "$SET_NAME_EFF" \
            --sport "$SPORT"

          # The fetcher exits 0 on a page whose shape changed but still parsed
          # thin. A checklist worth ingesting has rows; refuse before writing.
          ROWS=$(($(wc -l < "$OUT") - 1))
          echo "  csv rows=$ROWS"
          if [ "$ROWS" -lt 5 ]; then echo "REFUSE: only $ROWS rows parsed — not ingesting"; exit 1; fi

          COSMOS_CONNECTION_STRING=$(az webapp config appsettings list \
            --name HobbyIQ3 --resource-group rg-hobbyiq-dev \
            --query "[?name=='COSMOS_CONNECTION_STRING'].value" -o tsv | tr -d '\r\n')
          if [ -z "$COSMOS_CONNECTION_STRING" ]; then echo "COSMOS_CONNECTION_STRING empty"; exit 1; fi

          APPLY_ENV=""
          if [ "${APPLY_INPUT:-true}" = "true" ]; then APPLY_ENV="true"; fi
          COSMOS_CONNECTION_STRING="$COSMOS_CONNECTION_STRING" \
          COSMOS_DATABASE=hobbyiq \
          CSV_PATH="$OUT" \
          SOURCE_LABEL=hobbymonitor \
          APPLY="$APPLY_ENV" \
          node scripts/ingest-scraped-checklist.cjs

      - name: Report what is still stranded
        if: always()
        working-directory: backend
        run: |
          # The number that says whether this job is WORKING. A run can fetch
          # and ingest happily while stranded sales keep climbing, which is
          # exactly how the seed-queue drain went unnoticed for weeks — green
          # every time, moving nothing.
          COSMOS_CONNECTION_STRING=$(az webapp config appsettings list \
            --name HobbyIQ3 --resource-group rg-hobbyiq-dev \
            --query "[?name=='COSMOS_CONNECTION_STRING'].value" -o tsv | tr -d '\r\n')
          COSMOS_CONNECTION_STRING="$COSMOS_CONNECTION_STRING" node -e '
            const {CosmosClient}=require("@azure/cosmos");
            const c=new CosmosClient(process.env.COSMOS_CONNECTION_STRING)
              .database("hobbyiq").container("comps_staging");
            c.items.query({query:"SELECT c.status, COUNT(1) AS n FROM c GROUP BY c.status"})
              .fetchAll()
              .then(({resources})=>{
                for (const r of resources.sort((a,b)=>b.n-a.n)) {
                  console.log(String(r.n).padStart(9) + "  " + (r.status ?? "(none)"));
                }
              })
              .catch((e)=>{ console.log("depth check failed: " + e.message); });
          '
